# Skill 结构基线

- 最小单元：`<skill-name>/SKILL.md`
- 可选目录：`scripts/`、`references/`、`assets/`
- `SKILL.md` 必含 frontmatter：`name` + `description`
- `name` 与目录名一致（小写动词/短语）
- `description` 明确 WHAT + WHEN
- 主文件精简，细节下沉到 `references/`
