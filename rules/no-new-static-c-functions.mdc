---
description: 除非用户明确要求，禁止新增文件级 static C 函数（.m/.mm/.c）
alwaysApply: true
---

# 禁止随意新增 static C 函数

## 要求

在为本仓库编写或修改 **Objective-C / Objective-C++ / C** 源码时，**除非用户在对话中明确要求**（例如明确写「加一个 static 工具函数」），否则**不要新增**文件作用域的 **static C 函数**（含 `static inline` 的文件级函数）。

## 适用范围

- 针对业务手写实现：`.m`、`.mm`、`.c` 等。
- 第三方 vendored、工具生成片段若必须保留 static，不在此强制改写；**新写的业务代码**优先遵守本规则。

## 推荐替代

- 使用 **类方法**（`+`）、**实例方法**、**Category**、已有工具类或模块内已有抽象。
- 确需「仅本文件可见」时，优先与现有代码风格一致的做法（例如私有类、`@interface` 类扩展中的方法），而不是新加文件级 `static` C 函数。

## 示例

```objc
// ❌ 不推荐（用户未要求时新增）
static void CQDoSomething(void) { /* ... */ }

// ✅ 更推荐：类方法或实例方法等既有风格
+ (void)cq_doSomething { /* ... */ }
```

## 例外

用户在本轮对话中**明确指令**需要 `static` C 函数时，按用户要求实现即可。
