---
description: 仅当复杂逻辑多处重复时才抽取方法；简单重复片段不强制抽方法
alwaysApply: true
---

# 方法抽取：复杂才抽，简单不强行 DRY

## 原则

- **多处维护相同「复杂」逻辑**（分支多、边界多、易出错、易与产品口径漂移）时，应**抽取为单一实现**（方法 / 工具类 / 模块内复用点），避免拷贝粘贴导致不一致。
- **仅一两行、语义直白**的代码，即使在多个调用点出现，**也不要为了「复用」而强行抽方法**；重复少量简单代码通常比多一层间接更易读。

## 判断参考（满足越多越值得抽）

- 同一段逻辑超过约 **10 行**，或包含 **非平凡条件分支 / 状态组合**。
- 修改一处时，**客观上必须**同步改另一处，否则会出现行为不一致或缺陷。
- 涉及 **协议/安全/金额/权限** 等易错口径。

## 反例与正例

```objc
// ❌ 不推荐：为了一两行重复专门造方法，增加跳转与命名负担
- (void)foo { self.view.backgroundColor = [UIColor whiteColor]; }
- (void)bar { self.view.backgroundColor = [UIColor whiteColor]; }
// 不必因此新增 -cq_setWhiteBackground 之类封装，除非团队已有统一风格入口。

// ✅ 推荐：复杂校验/拼装/映射在多处出现时，抽到一处
// （示意）多处相同的字典拼装 + 校验 + 默认值合并 → 单一方法或 builder。
```

## 与「最小改动」的关系

新增抽取时**只服务当前重复或即将分叉的复杂逻辑**；不要顺带做大范围「洁癖式」重构。
